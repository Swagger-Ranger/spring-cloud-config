spring:
  application:
    name: spring-cloud-zipkin

server:
  port: 9411    #端口号为：9411，该端口号为 Zipkin Server 的默认端口号

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
      
management:
  metrics:
    web:
      server:
        auto-time-requests: false
